name: Publish beta release

on:
  push:
    branches:
      - publish-beta

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Check out release commit
        uses: actions/checkout@v4

      - name: Read package version
        id: version
        shell: bash
        run: |
          VERSION="$(python3 - <<'PY'
          import tomllib
          with open('pyproject.toml', 'rb') as handle:
              print(tomllib.load(handle)['project']['version'])
          PY
          )"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "tag=v$VERSION" >> "$GITHUB_OUTPUT"

      - name: Require beta version
        shell: bash
        run: |
          case "${{ steps.version.outputs.version }}" in
            *a*|*b*|*rc*) ;;
            *) echo "Refusing to publish non-beta version" >&2; exit 1 ;;
          esac

      - name: Create GitHub prerelease
        env:
          GH_TOKEN: ${{ github.token }}
          TAG: ${{ steps.version.outputs.tag }}
        shell: bash
        run: |
          if gh release view "$TAG" >/dev/null 2>&1; then
            echo "Release $TAG already exists; nothing to do."
            exit 0
          fi
          cat > release-notes.md <<'EOF'
          Linux beta for Grapher's human installation and release-aware update interface.

          Install on Linux:

          ```bash
          curl -fsSL https://raw.githubusercontent.com/seanbman/grapher/main/install.sh | bash
          ```

          The installer creates an isolated environment under `~/.local/share/grapher/venv` and links `grapher` into `~/.local/bin` without requiring sudo. Interactive launches query published GitHub releases and print a non-blocking notice when a newer version is available; offline and non-interactive use remains unaffected.

          This is a beta release. Existing long-form CLI commands and Grapher's canonical mutation model remain the automation contract while the Linux distribution flow is validated.
          EOF
          gh release create "$TAG" \
            --target "$GITHUB_SHA" \
            --prerelease \
            --notes-file release-notes.md \
            --title "Grapher $TAG (Beta)"
